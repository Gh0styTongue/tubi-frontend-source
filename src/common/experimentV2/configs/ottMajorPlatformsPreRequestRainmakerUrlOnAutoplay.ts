import type { ExperimentDescriptor } from './types';

export const ottMajorPlatformsPreRequestRainmakerUrlOnAutoplay: ExperimentDescriptor<{
  request_offset_v0: number;
}> = {
  name: 'webott_major_pre_request_rainmaker_url_on_autoplay_movie_v0',
  defaultParams: {
    request_offset_v0: 0,
  },
};
