import type { ExperimentDescriptor } from './types';

const webottMajorPlatformsAbortRainmakerRequestExitPlayerV0: ExperimentDescriptor<{
  enable_abort: boolean;
}> = {
  name: 'webott_majors_abort_rainmaker_request_exit_player_v0',
  defaultParams: {
    enable_abort: false,
  },
};

export default webottMajorPlatformsAbortRainmakerRequestExitPlayerV0;

