import { defineMessages } from 'react-intl';

const messages = defineMessages({
  modalDesc: {
    description: 'description text for remind modal',
    defaultMessage: 'We\'ll let you know when {programTitle} is streaming.',
  },
});

export default messages;
