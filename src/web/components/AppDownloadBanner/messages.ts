import { defineMessages } from 'react-intl';

const messages = defineMessages({
  text: {
    description: 'banner text',
    defaultMessage: '<b>Download the Tubi app</b> for a faster, smoother streaming experience.',
  },
});

export default messages;
