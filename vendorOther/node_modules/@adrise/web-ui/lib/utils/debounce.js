"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.debounce = void 0;

/**
 * Return a debounced function, with a static `clear` method to clear the internal timer
 */
var debounce = function debounce(fn, time) {
  var interval; // use any because setTimeout has duplicated type of `number` and `NodeJS.Timer`
  // tslint:disable-next-line:only-arrow-functions

  var result = function result() {
    clearTimeout(interval);

    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    interval = setTimeout.apply(void 0, [fn, time].concat(args));
  };

  result.clear = function () {
    clearTimeout(interval);
  };

  return result;
};

exports.debounce = debounce;
//# sourceMappingURL=debounce.js.map
